# 课间：项目约定

手机优先的本地课表与日程应用，React + TypeScript + Vite，兼容 PWA 和 Capacitor Android。保持现有模块结构，不因模型升级重建项目。

## 按任务获取上下文

- 安装、启动、数据限制和代码导航：查阅 `README.md` 对应章节；可执行命令以 `package.json` 为准。
- 恢复进度：读取 `PROGRESS.md` 顶部当前快照，历史段落仅在需要追溯时读取。旧阶段 Prompt 是历史需求，不是新任务授权。
- 业务：`src/domain/`；持久化与识别器：`src/services/`；页面与编辑器：`src/pages/`、`src/components/`。
- 界面：复用 `src/styles/tokens.css` 与 `DesignSystem.tsx`，保持灰白阅读区、深色控制面板、克制的信号黄及手机触控可用性。参考游戏图仅作视觉参考，不引入游戏资产。
- Android 打包：查阅 README 的 Android 章节及 `scripts/build-android-apk.ps1`；发布说明参考 `docs/RELEASE_NOTES_v1.0.0.md`，版本和远端状态在发布时重新核实。

## 关键边界

- 截图识别目前是可替换的模拟实现，界面继续明确告知用户，保留人工校对。真实 OCR 是独立需求，不在无关改动中偷偷接入上传服务。
- 保持本地数据兼容；修改存储模型时提供迁移并验证旧数据保留。不要清除真实用户的浏览器或应用数据来通过测试。
- 课程规则保存离散周次数组；个人日程独立建模。修改日期、合并、冲突或布局时检查相邻时间边界及重叠情况。
- 不读取或提交教务系统认证信息、Android 私钥和密码。构建脚本可以在授权打包任务中使用已有本地签名配置，不打印秘密。
- 本地实现不自动包含提交、推送、Release、改仓库公开性或覆盖既有发布；用户明确要求这些操作时，在其指定范围内完成。

## 完成与验证

选择与改动对应的检查：文档只检查内容、链接与格式；业务修改运行相关单元测试和类型检查；界面与交互修改检查手机尺寸和受影响流程；正式交付构建运行生产构建。涉及导入、存储、迁移或撤销时覆盖数据保留及失败路径。

常用命令：`npm run typecheck`、`npm run test`、`npm run build`、`npm run format:check`。完整 Web 回归可用 `npm run check`，再对最新构建运行 `npm run test:e2e`；无需重复执行已包含的检查。E2E 使用 Edge 和隔离测试数据，确认 4173 端口对应当前项目，勿中断未知进程。

本地隔离测试、构建和相关失败修复属于实现任务的常规步骤。检查通过后交付结果和剩余限制，不循环扩大测试。模拟器或浏览器验证不能描述为真机测试；用户要求打包后停下测试时，交付 APK 后停止。

需要暂停或交接时，更新 `PROGRESS.md` 顶部快照：已完成、实际验证、未完成和下一步；避免将历史状态描述成当前待办。
